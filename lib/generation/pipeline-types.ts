/** App-side re-export of the generation pipeline types (see @openmaic/generation). */
export type {
  AICallFn,
  AgentInfo,
  GeneratedSlideData,
  GenerationResult,
  SceneGenerationContext,
} from '@openmaic/generation';
