# Content-audit runner

One-off evidence tool (`npx tsx eval/content-audit/runner.ts [document.json]`) that reports
tier-1 content-integrity finding counts, token-free redundancy-proxy pairs,
spotlight-mismatch candidates (rare-word overlap) and figure-gap candidates for a
course document — the data used to decide which semantic judge classes are worth building.
